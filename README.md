# SCT_PE_3
Task 03 focuses on automating text summarization using AI. It converts unstructured text from different domains into a structured JSON format. The prompt extracts key points, creates a clear summary, and identifies action items for consistent output across fields.
